---
import type { HTMLAttributes } from 'astro/types';
import { row } from './Card.css';

const Tag = 'div';

interface Props extends HTMLAttributes<typeof Tag> {}

const { class: className, ...attrs } = Astro.props;
---
<Tag class:list={[row, className]} {...attrs}>
  <slot />
</Tag>
